extends layout

block content
  div(class='page')
    h1= title
    hr
    h2(class='redtext') Are you sure you want to delete the following contact?
    form(method='POST' action=`/contacts/${contact.id}/delete`)
      div(class='grid')
        label(class='section left') First Name:
        p(class='section right')=`${contact.firstName}`
        label(class='section left') Last Name:
        p(class='section right')=`${contact.lastName}`
        label(class='section left') Email:
        p(class='section right')=`${contact.email}`
        label(class='section left') Notes:
        p(class='section right')=`${contact.notes}`
      div(class='container')
        button(class='redBtn' type='submit') Confirm Delete
        a(class='greyBtn' href=`/contacts/${contact.id}`) Cancel
    p(class='container labTag') ~ ITMD 542 - Lab 3 - Bori Kim ~