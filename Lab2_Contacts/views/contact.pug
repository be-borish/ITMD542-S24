extends layout

block content
  h1= title 
  hr
  label First Name: 
    | 
  p=`${contact.firstName}`
  br
  label Last Name: 
    | 
  p=`${contact.lastName}`
  br
  label Email: 
    | 
  p=`${contact.email}`
  br
  label Notes: 
    | 
  p=`${contact.notes}`
  br
  label Date Created: 
    | 
  p=`${contact.dateCreated}`
  br
  label Date Updated: 
    | 
  p=`${contact.dateUpdated}`
  div.contact-actions 
    a(href=`/contacts/${contact.id}/edit`) Edit Contact
    | 
    a(href=`/contacts/${contact.id}/delete`) Delete Contact
    
    
